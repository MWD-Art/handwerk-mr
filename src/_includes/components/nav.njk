{% set currentUrl = page.url %}
<nav>
  {%- for entry in collections.all | eleventyNavigation -%}
  <a href="{{ entry.url }}" class="nav-link" {% if currentUrl===entry.url %} aria-current="page" {% endif %}>{{ entry.key }}</a>
  {%- endfor -%}
</nav>