<div class="news-grid">
  {% for post in posts | limit(5) %}
  <div class="news">

    <img src="https://picsum.photos/id/1{{ loop.index }}/900/600" alt="{{ post.title }}">

    <h3><a href="{{ post.url }}">{{ post.title }}</a></h3>
    <p>{{ post.text }}</p>

  </div>
  {% endfor %}
</div>